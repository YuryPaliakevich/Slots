package by.slots.service.slots.impl;

import org.springframework.stereotype.Service;

import by.slots.domain.SlotsResults;
import by.slots.domain.dto.SpinResponse;
import by.slots.service.slots.SlotsMapperService;

@Service
public class SlotsMapperServiceImpl implements SlotsMapperService {

    @Override
    public SpinResponse map(SlotsResults slotsResults) {
        return null;
    }

}
